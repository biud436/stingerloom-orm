name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: pnpm test

  build-check:
    name: Example Type Check
    runs-on: ubuntu-latest
    strategy:
      matrix:
        example: [nestjs-cats, nestjs-blog, nestjs-multitenant, nestjs-todo]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      - name: Install example dependencies
        working-directory: examples/${{ matrix.example }}
        run: pnpm install --frozen-lockfile

      - name: Type check
        working-directory: examples/${{ matrix.example }}
        run: npx tsc --noEmit

  # Phase 2: Integration tests with PostgreSQL + MySQL service containers
  # integration:
  #   name: Integration Tests
  #   runs-on: ubuntu-latest
  #   services:
  #     postgres:
  #       image: postgres:16
  #       env:
  #         POSTGRES_USER: test
  #         POSTGRES_PASSWORD: test
  #         POSTGRES_DB: test
  #       ports: ['5432:5432']
  #       options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
  #     mysql:
  #       image: mysql:8
  #       env:
  #         MYSQL_ROOT_PASSWORD: test
  #         MYSQL_DATABASE: fastify
  #       ports: ['3306:3306']
  #       options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=5
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: pnpm
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm build
  #     - run: INTEGRATION_TEST=true pnpm test
